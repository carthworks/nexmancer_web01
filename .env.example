# Public endpoint that receives contact-form submissions as JSON (POST).
# Expected payload: { name, email, company, subject, message, source }
# Must respond 2xx on success. Keep any API keys/secrets on that server — NOT here.
VITE_CONTACT_ENDPOINT=
